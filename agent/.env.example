# Required: Bearer token for API authentication (ottomator will send this)
API_BEARER_TOKEN=your_secret_token_here

# Required: OpenAI API key for AI analysis features
OPENAI_API_KEY=sk-your-openai-key-here

# Optional: LLM model to use (defaults to gpt-4o-mini)
LLM_MODEL=gpt-4o-mini

# Optional: Port to run on (defaults to 8001)
PORT=8001

# Optional: Supabase configuration for conversation history
# Only needed if deploying to ottomator Live Agent Studio
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-key-here
